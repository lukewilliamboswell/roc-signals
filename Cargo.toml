[workspace]
members = ["crates/gpui-host"]
resolver = "3"

[profile.dev]
debug = 0
panic = "abort"

[profile.release]
panic = "abort"
# Optimize the Rust host together; external platform libraries retain their
# independent releases and remain separate inputs to Roc's final link.
lto = "thin"
strip = "debuginfo"
