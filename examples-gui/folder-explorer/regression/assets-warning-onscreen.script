# size: 1200x820
# assets: regression/assets-problem
# diagnostic: GUI-10. The asset warning holds real area but is laid out below
# the window, so a person browsing at the standard size never sees that a glyph
# is missing. The line itself is fixed — it is the explorer's overall height
# and overflow that put it out of reach.
wait 600
expect-text Problem assets: glyphs/folder.png (altered), glyphs/file.png (missing). Startup check only: glyphs the host cannot load show placeholder boxes. Restart to re-check after restoring them.
expect-onscreen #asset-status
