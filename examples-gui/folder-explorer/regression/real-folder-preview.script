# size: 1200x820
# choose: regression/fixtures/project
# The sample workspace answers every action from memory. A real folder goes
# through the native listing, preview and open workers, which is where the
# Windows review found Preview text and Open in app inert (GUI-32).
click "Choose folder"
wait 800
expect-count entry: 4
click note.txt
wait 200
expect-enabled "Preview text"
click "Preview text"
wait 800
expect-value "Text preview" First line only
snapshot previewed
click crlf.txt
wait 200
click "Preview text"
wait 800
expect-missing No preview loaded.
snapshot crlf-previewed
