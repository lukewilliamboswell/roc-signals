# size: 360x600
# diagnostic: GUI-04. The discard dialog keeps its intrinsic content width, so
# at the narrowest window the host permits its heading and safe action are laid
# out past the left edge and its explanation past the right.
type "Note text" hi
wait 400
click New
wait 400
expect-onscreen #discard-confirmation
