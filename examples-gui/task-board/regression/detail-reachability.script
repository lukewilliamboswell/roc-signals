# size: 1200x820
# diagnostic: GUI-03. At the review size the detail panel is 1047 logical
# pixels tall inside an 820 pixel window, so its movement and deletion controls
# are laid out below the viewport. The board region clips both axes and the
# detail panel declares no scroll region of its own.
expect-onscreen #task-detail
