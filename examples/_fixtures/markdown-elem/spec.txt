# Markdown Elem structural fixture

expect_visible role:heading name:"Markdown Elem Fixture"
expect_visible text:"Release note"
expect_visible text:"bold outcome"
expect_visible text:"Signal.map"
expect_visible text:"First item"
expect_visible text:"Second item"
expect_visible text:"Quote text"
expect_visible text:"Allowed link"
expect_attr test_id:"allowed-link" href "https://example.test/release"
expect_visible text:"Blocked script"
expect_no_attr text:"Blocked script" href

# Static view: nested list, fenced code block, and image allowlist.
expect_visible text:"Nested detail"
expect_visible test_id:"static-code"
expect_visible text:"roc check app.roc"
expect_attr test_id:"static-image" src "https://example.test/badge.png"
expect_attr test_id:"static-image" alt "Build badge"
expect_visible text:"Bad badge"
expect_no_attr text:"Bad badge" src

# Live view mounts prefilled with a fence, a nested list, and images.
expect_visible test_id:"b:0:codeblock"
expect_visible text:"live fence body"
expect_visible text:"Alpha item"
expect_visible text:"Beta nested"
expect_attr test_id:"s:0:image" src "https://example.test/live-badge.png"
expect_visible text:"Nope image"
expect_no_attr text:"Nope image" src

mark_metrics
fill label:"Markdown input" "Updated **copy** with `inline code`\n[Live link](https://example.test/live)\n[Bad link](javascript:alert)"
expect_value label:"Markdown input" "Updated **copy** with `inline code`\n[Live link](https://example.test/live)\n[Bad link](javascript:alert)"
expect_visible text:"Updated "
expect_visible text:"copy"
expect_visible text:"inline code"
expect_visible text:"Live link"
expect_attr test_id:"s:5:link" href "https://example.test/live"
expect_visible text:"Bad link"
expect_no_attr text:"Bad link" href
expect_metric_delta_at_most patches_emitted 80
expect_metric_delta_at_most active_graph_records_rebuilt 24

# Live view: image allowlist under typing.
mark_metrics
fill label:"Markdown input" "Photo ![Shot](https://example.test/shot.png) and ![Evil](javascript:x) end"
expect_attr test_id:"s:1:image" src "https://example.test/shot.png"
expect_attr test_id:"s:1:image" alt "Shot"
expect_visible text:"Evil"
expect_no_attr text:"Evil" src
expect_metric_delta_at_most patches_emitted 80
expect_metric_delta_at_most active_graph_records_rebuilt 24
