# Documents what nodes_recomputed actually counts.
#
# One click dirties one source root and recomputes five derived nodes
# (four chain steps plus the constant). nodes_recomputed reports the roots,
# derived_calls_into_roc reports the derived evaluations.

expect_text test_id:"chain" "Chain: 4"
expect_text test_id:"constant" "Constant: unchanged"

mark_metrics
click role:button name:"Bump"
expect_text test_id:"chain" "Chain: 5"
expect_metric_delta nodes_recomputed 1
expect_metric_delta derived_calls_into_roc 7
expect_metric_delta propagation_prunes 1
expect_metric_delta patches_emitted 1
