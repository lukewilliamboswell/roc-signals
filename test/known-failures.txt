# Known failing example specs and wasm mounts. See scripts/known_failures.py.
#
# This list only shrinks: a spec that starts passing must be deleted here
# (`python3 scripts/test.py ... --update-known-failures` does it), and a spec
# that starts failing is a regression that fails the run. Adding a line is a
# deliberate, reviewed decision and needs a comment saying why.
#
# Empty since 2026-08-30. The last two entries went together: the spreadsheet
# rebind budget started passing when the engine stopped refreshing the whole
# grid, and the query-builder entry was a patch-count bound nobody could still
# justify, removed along with every other one. Keep it empty by fixing what
# fails; a line here is a bug being accepted, and it needs a comment saying
# which one and why.
