# Wasm size budgets checked by `python3 scripts/test.py size`.
#
# Generated from a reproduced baseline with 1% headroom above the
# measured value. Budgets are explicit numbers so a regression fails loudly.
# Increasing a budget is a deliberate, reviewed decision: explain the cause
# (compiler upgrade, accepted feature cost) in the pull request. Never
# regenerate this file automatically to bless a larger artifact, and never
# derive it from an instrumented or profiler build.
#
# Regenerate after an accepted change with:
#   python3 scripts/wasm_size.py --write-budgets .test-out/size/<label>/report.json

[baseline]
revision = "a53e6f884591def7b1e860f393d040fa6a6bacac"
roc = "Roc compiler version nightly-2026-09-04-c125b82"
zig = "0.16.0"
compression = "node-zlib"
headroom_percent = 1

[fixtures.static-text]
raw = 703105  # measured 696144
gzip = 264281  # measured 261665

[fixtures.counter]
raw = 710571  # measured 703536
gzip = 267343  # measured 264697

[fixtures.branch-replacement]
raw = 717973  # measured 710865
gzip = 269344  # measured 266678

[fixtures.keyed-row-edits]
raw = 1410301  # measured 1396338
gzip = 490251  # measured 485398

[fixtures.two-row-types]
raw = 1417622  # measured 1403587
gzip = 485678  # measured 480870

[fixtures.js-framework-benchmark]
raw = 1419706  # measured 1405650
gzip = 493718  # measured 488830

[fixtures.recipe-scaler]
raw = 1268487  # measured 1255928
gzip = 440708  # measured 436345
