+++
title = "API Request Console"
description = "A deterministic API console for POST requests, retries, non-2xx responses, and network failures."
template = "example.html"
[extra]
slug = "api-request-console"
+++

The console sends package-aligned HTTP requests and renders the response as an
app-facing request inspector. The native specs and browser demo both cover
success, non-2xx, retry, and failure paths through the same task source.
